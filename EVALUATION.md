# 评估记录：在公开数据集上的第一次真实测量

日期：2026-09-13
数据集：`codernotme/face_shape`（HuggingFace，MIT，免登录）
样本：200 张（5 类 × 40：oval / round / square / heart / oblong）
运行方式：`scripts/fetch-eval-dataset.mjs` → `scripts/batch-analyze.mjs --dir .eval`（headless 浏览器跑真实页面）

---

## 结果

| 指标 | 值 |
|---|---|
| top-1 一致率 | **1.5%（3/200）** |
| top-2 一致率 | 21.0%（42/200） |
| 预测为 diamond 的比例 | **≈88%（175/200）** |

按类别：oval 0/40、round 0/40、square 2/40、heart 1/40、oblong 0/40。

## 发现一：菱形规则被数据证伪（真 bug，必须修）

88% 的图片被判成 diamond。原因不是锚点，而是**我把一个普遍特征当成了判别特征**：

```js
diamond: band(r4, 0.68, 0.95) + band(r3, 1.12, 1.70) + band(r2, 0.88, 1.12)
```

真实人脸里「颧骨比额头中部宽（r4 < 0.95）、比下颌宽（r3 > 1.12）」**几乎人人成立**，
所以这个规则对所有脸都给高分。而唯一测过的那张脸（用户本人）恰好真是菱形，
**单张测试永远发现不了它** —— 这就是批量数据的价值。

同时确认：置信度经常 76–97% 却是错的 → **conffidence 度量的是自洽性，不是正确性**。

## 发现二：锚点不是主因

| 锚点组 | top-1 |
|---|---|
| 额宽 → 67/297 | 26.5% |
| 额宽 → 109/338 | 26.5% |
| 颌宽 → 132/361 | 25.0% |
| **当前默认** | **1.5%** |

但前两行"更好"是**假象**：67/297 在头顶附近，量出 r4 ≈ 0.55，恰好掉出菱形区间下界 0.68，
于是**碰巧让坏规则不触发**。把锚点当证据会得到完全错误的结论。

即使最好的锚点也只有 26.5%，远低于公开基准（`metadome/face_shape_classification` 报 85.3%，
Kaggle 上那个方法自报 85%）—— 但那些是**训练过的 CNN，而且是 5 类问题**（不含 diamond）。

## 发现三：这个数据集不能作为准确率基准

逐类比例分布**几乎完全重叠**：

| 比例 | heart | oblong | oval | round | square |
|---|---|---|---|---|---|
| r1 长/颧宽（中位数） | 1.183 | 1.177 | 1.167 | 1.188 | 1.161 |
| r2 额/颌宽 | 0.911 | 0.907 | 0.903 | 0.882 | 0.869 |
| r3 颧/颌宽 | 1.267 | 1.245 | 1.256 | 1.235 | 1.221 |
| r4 额/颧宽 | 0.722 | 0.726 | 0.716 | 0.708 | 0.708 |

四个比例对标签**零区分力**。人工查看图片后确认：

1. **图片是紧密裁切的黑白名人肖像，190×250。** 这一点已目视确认。
2. **标签来源未知**（只有一行 license，无标注说明）—— 这类数据集通常是网络图片按搜索词归类，
   即 "oblong face celebrities" 这种查询的搜索结果，标签本身有噪声。

### 一个被数据否掉的假设（记录在此，避免以后重犯）

我最初的解释是「裁切把发际线切掉了，导致脸长被压缩」。**已检验，不成立：**

| 检查 | 评估集（200 张） | 用户本人正常拍摄（5 张） |
|---|---|---|
| 发际线一致性中位数 | **0.803** | 0.823 |
| 发际线告警触发 | 0 / 200 | — |

两者一致。若发际线真被裁掉，网格顶端会落低，这个比值应当**升高**。它没有。
所以裁切没有破坏脸长测量。

**真正观察到的事实（不加解释）：** r1 全样本范围 **1.02–1.36**（中位 1.180），
而按定义 oblong 应当 ≥ 1.6，实测其 r1 中位数 1.177 与 round 的 1.162 几乎相同。
**为什么会这样，目前没有证据支持任何一个解释** —— 可能是标签不反映可测量的几何，
也可能是本工具的「宽度」与标注者的「宽度」不是同一个量。要分清，需要一份
**有明确标注说明、且取景与真实用户一致**的数据。猜测无益。

**所以「1.5%」不能表述为"本工具的准确率"** —— 它测的是"在这批来源不明的标签上的一致率"。
反过来，把它藏起来也是错的。

## 已完成的整改

### A. 修结构性缺陷（已完成，两轮）

**第一轮：把"绝对区间"换成"人群相对判据"。**
原来我手写的区间要求 r1 能到 2.30、r3 能低到 0.98、r4 能超过 1.00 —— 实测数据里这些区域
**一个都不存在**，于是 6 个类别里 5 个不可达、diamond 通吃 88%。
现在所有「宽 / 窄」都用 `POPULATION` 里的**人群分位数**定义（`classify.ts`），
**只用比例数值、不用标签**。

**第二轮：把下颌角从"过滤器"改成"判别器"。**
原来写成「≤137 算方、≥140 算圆」，而人群中位数恰好是 **136.4** —— 两个判据都卡在中位线附近
半触发，圆脸被压到 1.5%，方脸却靠 `r3 ≤ p75`（覆盖 75% 的人）涨到 30%。
现在取下颌角的 p25 / p75，并收紧方脸的 r3 项。

| 指标 | 整改前 | 整改后 |
|---|---|---|
| 可达类别 | **1 / 6** | **6 / 6** |
| diamond 预测占比 | **88%** | 10% |
| top-1 | 1.5% | **30.0%** |
| top-2 | 21.0% | **49.5%** |
| 预测分布 | 单类通吃 | heart 26% · oblong 26% · square 20.5% · oval 15% · diamond 10% · round 2.5% |

**关于这 30% 必须说清楚：** 5 类问题的随机基线是 20%，30% 只是**勉强高于随机**；
而且这批标签本身不可信（见上），所以**这不是本工具的准确率**。
上面的整改价值在于**修掉了结构缺陷**（类别可达、没有类别通吃），而不是"提高了准确率"。

**已知未解决的限制：`round` 仍只占 2.5%（5/200），该类正确率 0/40。**
刻意停在这里：继续调就是拿「圆脸应该更常见」这个**先验**去凑分布，
而这批标签没有任何信号能支持它。这个限制应当如实写进方法页，而不是靠调参掩盖。

### C. 改成「测量值优先」的产品表述（已完成）

- 工具页结果区改为：**先给四个测量值 + 直白读法**（"颧骨比下颌宽约 27%"）+ **最宽点**，
  再给「最接近的类别」，并附一句"类别是约定，测量才是数据"。
- 首页首段改为以测量为主语。
- 方法页：把实测的 30% / 50% 写进 FAQ，并新增两条限制
  （四比例动态范围窄；「额宽」在不同资料里定义不一致）。

### B. 有效的准确率基准（**受阻，需要真实照片**）

公开数据集这条路已经试过并且**失败**（原因见上）。剩下的可行路径：

1. **自己拍**（`CALIBRATION.md`）：发际线可见、非裁切、正面、手臂长度。
   成本最低、最贴近真实用户，但需要若干人的照片。
2. 找一份**有明确标注说明、且取景与真实用户一致**的数据集。目前没找到。

**所以 B 暂时无法完成** —— 它需要的输入（多脸型的合格照片）不在我手里。
在拿到之前，方法页上那个 30% 应当保持现状，不要改写成更好看的数字。

## 复现方式

```powershell
.\dev.ps1 run fetch:eval -- --per-class 40   # 下载评估集（需要网络）
.\dev.ps1 run batch -- --dir .eval           # 跑评估（需要 dev server 在跑）
.\dev.ps1 run analyze:eval                   # 打印逐类比例分布
```

评估图片在 `.eval/`，已 gitignore，只用于本地评估，不再分发。
