# 阈值校准手册

**这是当前项目最大的质量缺口。** `landmarks.ts` 的锚点索引和 `classify.ts` 的分类区间都是
「合理默认」，来自面部测量学的通用定义 —— 但它们**没有在你的数据上验证过**。
脸型分类的准确率完全取决于这一节，代码写得再干净也替代不了。

目标：把「我觉得应该用这个区间」变成「在这 28 张照片上 top-1 一致率 79%，top-2 93%」。

---

## 一、先建回归集

**20–30 张照片起步**，每张都必须有一个你信得过的「正确答案」。

### 照片从哪来

| 来源 | 可用性 |
|---|---|
| 你自己的照片（多个角度、多个时期） | ✅ 最安全，但脸型类别可能不全 |
| 亲友的照片，**取得对方同意** | ✅ 可以，注意只用于本地测试、不进 git |
| 公开资料中明确标注过脸型的名人 | ⚠️ 只做本地测试，**绝对不要把照片提交进仓库**，也不要用在成品站上 |
| AI 生成的合成人脸 | ⚠️ 可以用，但比例偏理想化，容易把阈值调偏 |

**不要把这个文件夹提交进任何仓库。** 真实人脸的图片涉及肖像权，即使是测试数据也一样。
`regression/` 已经加进 `.gitignore` —— 保持这样。

### 类别覆盖

尽量凑够六类，每类 3–5 张。如果某一类只有 1 张，你就无法区分「阈值不对」和「这张照片特殊」。

### 标注规则（重要）

先定死判定标准，否则你会在标数据的过程中不断改标准，最后无法收敛。建议：

1. 先用尺子法量一遍（发际线→下巴 L、颧骨最宽 W、额宽 F、颌宽 J），记录 L/W 和 F/J；
2. 拿不准的照片，**明确标成「边界」**（比如 L/W 在 1.45–1.55 之间），
   评估时单独统计 —— 强行给边界照片贴单一标签，会让你的准确率数字变得毫无意义。

## 二、怎么记录：跑批处理，别手工填表

手工一张张截图记录会让人放弃。**整个过程已经自动化了** —— 你只需要写标注文件，
剩下交给 harness。

### 你要做的

1. 照片丢进 `regression/`（命名随意），格式要求见 `regression/README.md`
2. 在 `regression/labels.json` 里标注正确答案（照抄 `regression/labels.example.json`）：

```json
{
  "a.jpg": { "shape": "oval" },
  "b.jpg": { "shape": "square", "person": "A" },
  "c.jpg": { "shape": "oval", "borderline": true }
}
```

3. 跑一次：

```powershell
.\dev.ps1 run batch
```

### harness 会自动给你什么

- `regression/results.json` / `results.csv` —— 每张照片的完整数据：
  输出脸型、置信度、runner-up、四个比例、下颌角、姿态、告警
- 终端直接打印三个指标（就是下面这三个）
- **按标注脸型细分** —— 一眼看出是哪一类在拖后腿，以及被误判成了什么
- **稳定性报告** —— 同一个人多张照片的结论分布（`person` 字段）

三个指标的判定标准（CALIBRATION.md 第五节有目标值）：

- **top-1 一致率** = 匹配数 ÷ (总数 − 边界数)
- **top-2 一致率** = 含 runner-up 的匹配数 ÷ (总数 − 边界数)　←　更贴近真实体验，
  因为产品上你会同时展示次优解
- **边界告警覆盖率** = 边界照片中被 `low_confidence` 覆盖的比例　←　这个比准确率更重要：
  宁可承认不确定，也不要自信地给错答案

每轮调完参**把终端指标抄到下面对应的一行** —— 别覆盖旧数据，否则你不知道改动是改进还是退步。

| 轮次 | 改了什么 | top-1 | top-2 | 边界覆盖 | 稳定人数 | 备注 |
|---|---|---|---|---|---|---|
| 0 | 初始阈值（未校准） | — | — | — | — | 见下方「已有人工确认的样本」 |
| **1** | **修正菱形/心形规则方向 bug**；`hairlineRatio` 阈值 0.82→0.90 并改语义 | **5/5 (100%)** | 5/5 | — | 1/1 稳定 | 见下方「第 1 轮：本人 5 张」 |
| 2 | | | | | | |

### 第 1 轮：本人 5 张（一次一个变量：基线/盖额头/近距离/侧转/阴影光）

**测量稳定性 —— 优良。** 这是本轮最重要的结论：

| 比例 | 5 张的范围 | 标准差 |
|---|---|---|
| 脸长÷颧宽 | 0.098 | 0.032 |
| 额宽÷颌宽 | 0.043 | 0.016 |
| 颧宽÷颌宽 | 0.039 | 0.017 |
| **额宽÷颧宽** | **0.020** | **0.008** |

结论：5/5 判成菱形（置信度 76–80%），结论一致。**测量管道稳定，地基可靠。**

**姿态门禁 —— 已验证有效。** 侧转那张的 yawProxy = −0.1746，是阈值 0.055 的 3.2 倍，
被正确拦下；其余四张 −0.016 ~ −0.044 全部通过。roll 全部在 ±2.2° 内。

**发际线检查 —— 被证伪，已降级。** 5 张的 `hairlineRatio` =
`0.7946 / 0.8243 / 0.8325 / 0.8241 / 0.8244`，全部挤在 0.038 宽度内，
而原阈值 0.82 从中间劈开（80% 误报）。且该值与是否遮挡额头**无关**：
被遮挡那张的值落在正常照片的取值范围之内 → **零区分能力**。
已把阈值改为 0.90 并改语义为「顶部锚点极端值兜底」。
要重新做成真正的遮挡检测，需要一批**已知遮挡/未遮挡**的标注照片。

**待办**：`yawProxy` 在这 5 张里全部为负（−0.016 ~ −0.175），像是有约 −0.03 的系统偏置。
如果是真的，说明 `±0.055` 的容差在实际使用中是**不对称**的。需要更多人的数据确认。

### 已有人工确认的样本

数量少但**标签可信**（本人确认），比一堆猜的标签有价值：

| # | 照片 | 人工确认 | 工具输出 | 用的锚点 | 结论 |
|---|---|---|---|---|---|
| 1 | 用户本人（露额头正脸） | **菱形 diamond** | 菱形 76% | 默认 `103/332` | ✅ 对上 |
| 1b | 同上 | 菱形 diamond | 菱形 44%（次优心形 0.755） | 标准位置 `54/284`（−20.6%） | ⚠️ 类别对，但置信度低 —— 这个人**确实卡在菱形/心形边界**上 |

值得注意：**结论对锚点选择不敏感**（颧宽 4/4、额宽 4/5、颌宽 3/4 都给出菱形）。
这是好消息（说明测量稳健），但**不等于正确** —— 稳健地错也是可能的，样本量到 20+ 才能下结论。

## 三、调参顺序（必须按这个顺序）

顺序反了会互相干扰，永远调不收敛。

### 第 1 步：锚点（`ANCHORS`）

**在动任何阈值之前，先把锚点定死。**

打开 `/debug`，传照片，打开标签，逐张检查：

- `cheekLeft/Right (234/454)` 是否真的落在颧骨最宽处？还是落到了耳前/下颌上？
- `jawLeft/Right (172/397)` 是否落在下颌角上？
- `trichion (10)` 是否落在发际线上？还是被头发推低了？
- **悬停看偏移**：鼠标划过脸上任意点，页面顶部会读出 `#索引 · 眼线下 +X%`。
  参考量级（面部三等分）：眉约 −7%、鼻底约 +27%、下巴约 +60%。
  **颧弓应该在眼线下不远处**；如果某组候选的偏移贴近嘴或下颌，那它量的是「轮廓最宽处」而不是颧弓
- **点位探测器**：找到候选索引后填进输入框钉住它，方便截图和反复对照

然后用**候选锚点灵敏度表**比较：同一组候选，在不同照片之间按颧宽归一化的宽度波动越小越稳。
把波动大的换掉，改 `ANCHORS`，回 `/debug` 复查。
改完锚点后跑一次 `.\dev.ps1 run batch`，看 top-1 是否上升 —— **不要只看单张照片就下结论**。

> 判据：**同一张脸的不同照片**，三个比例应该接近。如果换个角度 r3 就从 1.05 跳到 1.30，
> 那是锚点不稳，不是脸变了。

### 第 2 步：姿态门禁（`pose.ts` 的 `POSE_LIMITS`）

先松后紧。把门禁放宽到几乎不触发，收集一批「明显歪头」的照片，看它们的 `rollDeg` /
`yawProxy` / `pitchProxy` 实际落在什么范围，再据此收紧 —— 目标是**让歪头照片全部被拦下，
同时正脸照片一张都不误伤**。误伤（正脸被拒）比漏放更伤体验。

### 第 3 步：分类区间（`classify.ts` 的 `band()`）

现在才开始调区间。方法：

1. 把回归集所有照片的 `r1 / r2 / r3 / r4 / 下颌角` 导出（`/debug` 的 JSON 里有）；
2. 按标注脸型分组，看每一类的判别量实际落在什么区间；
3. **用数据画区间，而不是用你的直觉**。两类重叠的部分，就是天然的边界区，
   不要试图硬切 —— 把重叠区留在两个规则之间，让 runner-up 去处理。

### 第 4 步：下颌角（最弱的一环）

`square` 和 `round` 的 r1/r2/r3 几乎一样，唯一的分开依据是下颌角。
如果第 3 步做完发现这两类始终混在一起：

- 先确认 `172/397` 是不是真的落在下颌角上（第 1 步没做扎实就会这样）；
- 如果锚点没问题但还是分不开，**接受它** —— 把这两类的 `angular`/`rounded` 权重降低，
  让它们在边界时互相作为 runner-up，同时在 UI 上明确提示「方脸 / 圆脸的区分对角度很敏感」。
  这比强行调出一个假的高准确率诚实得多。

### 第 5 步：置信度映射（`assess()`）

最后调。把 `confidence` 和「top-1 是否正确」对照一下：confidence 高的那批是不是真的更准？
如果不是，说明你的映射没意义，重新设计（而不是调系数调到看起来像）。

目标是让 `low_confidence` 告警成为**有效的不确定性信号**，而不是一个随机出现的烦人提示。

## 四、稳定性测试（容易被忽略，但对口碑影响最大）

准确率是一回事，**同一张脸给出稳定结果**是另一回事。用户会拿两张照片试，结论不一致就再也不来了。

做法：挑 3 个人，每人拍 5 张（不同角度 / 距离 / 光线 / 有头发遮挡 vs 拨开），
记录结论分布。

| 人 | 5 张照片的结论 | 稳定? |
|---|---|---|
| A | oval ×4, oblong ×1 | 基本稳定 |
| B | round ×2, square ×2, heart ×1 | ❌ 需要排查 |

不稳定时按优先顺序排查：① 发际线是否被头发影响 ② 姿态门禁是否漏放了侧脸 ③ 锚点是否不稳。
**通常问题出在告警没有拦住该拦的照片，而不是分类算法本身。**

## 五、什么时候算调够了

**先看外部基准，别把目标定错。** 这个任务公开报道的准确率就在 **82–85%** 这个量级：

- Kaggle 上那个公开的脸型数据集/方法叫 `face-shape-detection-85-acc-on-test-set`（**85%** test accuracy）
- HuggingFace 上基于它训练的 FastAI 模型 `lagerbaer/female_face_shapes` 自报 **约 82%**
- 头部商业厂商（Perfect Corp，10 万+ 人脸训练集）写了一篇标题叫「Average Accuracy?」的文章，
  **正文里一个数字都没给** —— 只说"取决于训练数据/照片条件"

所以：**"95% 准确"的说法在这个品类里一定是营销话术**；85% 左右就是当前实践的上限。
下面这些目标值是按这个基准定的。

不要追求 100%（脸型本来就是连续谱，追求 100% 只会导致过拟合到你的照片）。建议的停止条件：

- top-1 一致率 ≥ 75%，且
- top-2 一致率 ≥ 90%，且
- 边界照片的 `low_confidence` 告警覆盖率 ≥ 80%，且
- 稳定性测试中，同一人 5 张照片里至少 4 张结论一致

达到这些就可以上线，然后把回归集扩到 50+ 张继续迭代。**记录每一轮的指标**，
这样你才知道改动到底是改进还是退步。

## 六、别忘了产品层面

技术指标之外，还有两件同等重要的事：

1. **在 UI 上把不确定性说清楚**。技术上的 75% 准确率，配上「边界情况提示 + runner-up + 局限说明」，
   用户体验会好过 90% 准确率但从不承认不确定的竞品。
2. **不要虚假宣称精度**。别写「AI 精准测量」「准确率 99%」这类话 ——
   既站不住脚，也是广告审核和用户投诉的高发区。
