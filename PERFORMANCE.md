# 性能实测记录

**为什么单独记一份**：本站的性能瓶颈**不在首屏**，而标准测速工具（Lighthouse / PSI /
WebPageTest）**只测首屏**。首屏只有约 15 KB JS，谁测都是高分；真正让用户等待的是
「选完照片之后下载 wasm 运行时 + 模型」这一段，它由用户交互触发，那些工具看不到。
**所以本站必须用自己的脚本测，不能靠 Lighthouse 的分数。**

测量方式：`npm run measure:analysis <图片> [url]`
（真实浏览器 + Resource Timing API，冷/热/回访三种场景）

---

## 实测数字（2026-09-13，对线上 `face-shape-detector-enf.pages.dev`）

| 场景 | 到出结果 | 实际下载 |
|---|---|---|
| **① 首次访问（冷缓存）** | **4725 ms** | **6808 KB** |
| **② 同页再算一张（内存已热）** | **113 ms** | 0 KB |
| **③ 回访者（重载页面后）** | **1253 ms** | **0 KB（全部缓存命中）** |

### ① 的下载明细

| 资源 | 传输（压缩后） | 解压后 | 耗时 |
|---|---|---|---|
| `mediapipe/wasm/vision_wasm_internal.wasm` | **3021 KB** | 10892 KB | 1394 ms |
| `models/face_landmarker.task` | **3671 KB** | 3671 KB | 1276 ms |
| `mediapipe/wasm/vision_wasm_internal.js` | 77 KB | 314 KB | 271 ms |
| `_astro/vision_bundle.*.js` | 39 KB | 131 KB | 229 ms |

---

## 结论

### ✅ 缓存策略完全达成目标

`public/_headers` 里的 `max-age=31536000, immutable` **确实生效**：
回访者重载页面后，wasm 与模型**都是 transferSize = 0（缓存命中）**，
首次分析从 4725 ms 降到 1253 ms（约 3.8 倍）。剩下的 1253 ms 是 wasm 编译 + 初始化 + 推理。

**这正是当初把 wasm/模型放 `public/` 而不是走打包器 hash 的原因** —— 文件名稳定，才能设 immutable。

### ⚠️ 首次下载量的真实规模：约 7 MB，不是 3 MB

- wasm 运行时压缩后 **3021 KB**（解压后 10.9 MB —— 比模型还大，是最容易被忽略的一块）
- 模型 **3671 KB**（**几乎没有被压缩**：`.task` 是 TFLite bundle，传输量 ≈ 原始大小）
- **合计 6808 KB**

**曾经有 4 处用户可见文案写「约 3 MB」，与实测差一倍以上**（已修）：
工具页状态栏、方法页、About 页、首页 FAQ。慢速移动网络（1.5 Mbps）下 6.8 MB 需要**约 37 秒** ——
提示写着「约 3 MB」而用户等了 40 秒，只会以为坏了。

### 还没做的优化（按性价比，均未实施）

| 优化 | 收益 | 代价 |
|---|---|---|
| **在用户点开文件选择框时就预加载** | 文件选择对话框打开期间可并行下载，可能省下 1–2 秒 | 用户点了又取消会白下载 6.8 MB |
| **给模型下载加真实进度条** | 慢网体验明显改善 | MediaPipe 不暴露下载进度；要自己 `fetch` + 流式读取拿到进度，再用 `baseOptions.modelAssetBuffer` 交给它。wasm 那 3 MB 仍无进度可言 |
| **让模型被压缩传输** | 可能省几百 KB | `.task` 是 TFLite bundle，压缩收益未知，需要先实测；Cloudflare 对 `application/octet-stream` 未必自动压缩 |
| 换更小的模型/运行时 | 大幅减少首载 | 换掉 MediaPipe 就等于重做面部分析链路，与"测量稳定"的既有成果冲突 |

**结论：先把文案改对（已做）；优化留到有人反馈"第一次很慢"再说。**
在没有真实用户数据前做预加载/进度条，是在猜。
